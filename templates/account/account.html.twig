{% extends "base.html.twig" %}

{% block title %}Mon compte{% endblock %}

{% block body %}
    <div class="toaster">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} alert-dismissible fade show">
                    {{ message }}
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            {% endfor %}
        {% endfor %}
    </div>
    
    <div class="container edit mb-5">
        <h3 class="text-center">Bienvenue {{ app.user.username }}</h3>
        <div class="flex-sm-lg text-center">
            <a href="{{ path('account_profile') }}" class="btn btn-primary m-2">Modifier mes informations</a>
            <a href="{{ path('account_password') }}" class="btn btn-primary m-2">Modifier mon mot de passe</a>
            <a href="{{ path('account_wig') }}" class="btn btn-primary m-2">Mes réservations</a>
        </div>

    </div>

{% endblock %}