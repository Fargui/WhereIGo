{{ form_start(form) }}
    <div class="mt-5">
        {{ form_row(form.placeHasCategories) }}
       <!--  <pre>
            {{ dump(form.placeHasCategories) }}
        </pre> -->
        <button type="submit" class="btn btn-filter text-light">Valider les filtres</button>
    </div>
{{ form_end(form) }}


