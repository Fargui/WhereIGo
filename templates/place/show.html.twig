{% extends 'base.html.twig' %}

{% block title %}Votre prochain voyage{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1>{{place.name}}</h1>
        <a href="{{ path('booking_create', {'id': place.id}) }}" class="btn btn-primary">Réserver</a>
    </div>
{% endblock %}
